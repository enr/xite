
apply plugin: 'application'

applicationName = 'xite'
mainClassName = 'xite.XiteMain' //com.github.enr.xite.Main'

artifacts {
    archives distZip
    distZip.outputs.files.each { output ->
        archives file("${output.getAbsolutePath()}.MD5")
    }
}

